# Server port is arbitrary
server.port=8082

# Data Source Setup
# Note: Users must create a H2 db that they will reference here, create a user that has access to it on localhost
#       and provide the credentials below to access it

# uncomment the below lines to view sql output in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/criiterchonologer?useSSL=false&serverTimezone=UTC
spring.datasource.username=sa
spring.datasource.password=@Tireallife1023
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# to auto create Database
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=create-drop

spring.main.allow-bean-definition-overriding=true